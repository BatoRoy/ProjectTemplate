{
  "name": "app-client",
  "version": "0.1.0",
  "description": "Desktop client",
  "author": "BatoRoy",
  "license": "MIT",
  "private": true,
  "main": "electron/main.js",
  "scripts": {
    "dev": "concurrently -k -n vite,electron -c blue,yellow \"npm run dev:renderer\" \"wait-on http://localhost:5311 && electron .\"",
    "dev:renderer": "npm run dev --prefix frontend",
    "build:renderer": "npm run build --prefix frontend",
    "package:linux": "npm run build:renderer && electron-builder --linux AppImage --publish=never",
    "package:win": "npm run build:renderer && electron-builder --win nsis --publish=never",
    "package": "npm run build:renderer && electron-builder --linux AppImage --win nsis --publish=never",
    "publish:linux": "npm run build:renderer && electron-builder --linux AppImage --publish always"
  },
  "build": {
    "appId": "com.example.app",
    "productName": "App",
    "copyright": "Copyright © 2026 BatoRoy",
    "asar": true,
    "directories": {
      "buildResources": "build",
      "output": "../dist/electron"
    },
    "files": [
      "electron/**/*",
      "frontend/dist/**/*",
      "package.json"
    ],
    "publish": {
      "provider": "s3",
      "endpoint": "http://CHANGE-ME-SERVER-IP:9000",
      "bucket": "releases",
      "path": "apps/app"
    },
    "linux": {
      "target": [
        {
          "target": "AppImage",
          "arch": [
            "x64"
          ]
        }
      ],
      "icon": "build/appicon.png",
      "category": "Utility",
      "executableName": "app"
    },
    "win": {
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        }
      ],
      "icon": "build/windows/icon.ico"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true
    }
  },
  "dependencies": {
    "electron-updater": "^6.3.9"
  },
  "overrides": {
    "yauzl": "^3.3.1"
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "electron": "^42.0.0",
    "electron-builder": "^26.8.1",
    "wait-on": "^8.0.0"
  }
}
